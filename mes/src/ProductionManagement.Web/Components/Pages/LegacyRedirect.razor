@page "/lots"
@page "/wip-matrix"
@inject NavigationManager Nav

@* 예전 웹 임시 화면("LOT 조회", "재공 현황") 주소를 앱과 같은 "입 · 출고 현황 조회"로 보낸다(2026-09-15 웹 QA #15).
   두 화면은 앱에 없는 구성이라 삭제했다 - 같은 정보(LOT 목록·재공 매트릭스)는 입 · 출고 현황 조회에 있다. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("lot-inout", replace: true);
}
